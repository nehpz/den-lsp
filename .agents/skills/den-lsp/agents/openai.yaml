interface:
  display_name: "Den Lsp"
  short_description: "Repo-specific patterns and workflows for den-lsp"
  default_prompt: "Use the den-lsp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true